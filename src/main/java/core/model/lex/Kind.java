package core.model.lex;

public enum Kind {

    // lexemes
    IDENTIFIER,
    NUMBER,
    EQUAL,

    // Keyword
    DECLARE,
    MULTIPLY, PLUS, MINUS, DIVIDE, REMAINDER, NEW_LINE, READ_OPERATION, DISPLAY_OPERATION, INTEGER

}
